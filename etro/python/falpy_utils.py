
import numpy as np
import tensorflow as tf

#def denseToMatrix(layer):
#  pass

#def modelToMatrix(model, layer_types):
#  trainable_variables = [layer.trainable_weights for layer in model.layers]
  
  # Put proper check in later but for now note that layer_types must equal 
  # model.layers

#Gonna try and do this without python, lets see.
